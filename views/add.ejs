<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Thought</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f0f0f0;
            font-family: Arial, sans-serif;
        }

        .registration-container {
            max-width: 400px;
            margin: auto;
            padding: 30px;
            border-radius: 10px;
            background-color: #ffffff;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .registration-container h2 {
            color: rgb(10, 86, 2);
            font-family: 'Times New Roman';
            font-weight: bold;
            margin: 20px;
        }

        .form-group label {
            font-weight: bold;
            color: #555;
            padding: 5px;
            font-size: 18px;
            font-family: sans-serif;
        }

        .form-control {
            border-radius: 5px;
            border-color: #ccc;
            padding: 12px;
            font-size: 14px;
        }

        .btn-register {
            background-color: #27ae60;
            /* Green color */
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            color: #fff;
            font-weight: bold;
            width: 100%;
            cursor: pointer;
            margin-top: 20px;
            transition: background-color 0.3s;
        }

        .btn-register:hover {
            transition: all 0.5s;
            color: white;
            background-color: #219c52;
            /* Darker green color on hover */
        }
    </style>
</head>

<body>
    <section class="mt-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="registration-container">
                        <h2 class="head">Add Thought</h2>
                        <form class="form-horizontal" enctype="multipart/form-data" method="post" action="/blogData">
                            <div class="form-group">
                                <label for="image" class="control-label col-form-label">
                                    Image</label>
                                <input type="file" name="image" class="form-control" />
                            </div>
                            <div class="form-group">
                                <label for="title" class="control-label col-form-label">
                                    Title</label>
                                <input type="text" name="title" class="form-control" id="fname" />
                            </div>
                            <div class="form-group">
                                <label for="description" class="control-label col-form-label"> Description</label>
                                <textarea class="form-control" name="description"></textarea>
                            </div>
                            <div class="border-top border-dark">
                                <div class="card-body">
                                    <button type="submit" class="btn btn-register">Submit</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>

</html>
